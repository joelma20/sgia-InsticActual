{% extends 'base.html.twig' %}

{% block title %}Mostrar Semestre{% endblock %}

{% block body %}
<div class="col-sm-8">
    <div class="card">
        <div class="card-header">
            <h4> Semestre {{ semestre.nomeSemestre }}</h4>
            <div class="card-body">
                <table class="table table-sm ">
                    <tbody>

                    <tbody>
                    <tr>
                        {{ include('semestre/_delete_form.html.twig') }}
                        <a href="{{ path('semestre_index') }}" class="btn btn-sm btn-primary mr-2 ml-2"><i class="fas fa-list"></i>Voltar a Lista</a>
                        <a href="{{ path('semestre_edit', {'id': semestre.id}) }}" class="btn btn-sm btn-secondary"><i class="fas fa-edit"></i>Editar</a>
                    </tr>
                    <tr>
                        <th>Nome Semestre</th>
                        <td>{{ semestre.nomeSemestre}}</td>
                    </tr>
                    </tbody>
                </table>

            </div>
        </div>
    </div>

</div>


{% endblock %}




