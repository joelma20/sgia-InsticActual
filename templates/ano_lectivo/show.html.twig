{% extends 'base.html.twig' %}

{% block title %}Mostrar Ano Lectivo{% endblock %}

{% block body %}

<div class="col-sm-8">
    <div class="card">
        <div class="card-header">
            <h4> Ano Lectivo {{ ano_lectivo.nomeAnoLectivo}}</h4>
            <div class="card-body">
                <table class="table table-sm ">
                    <tbody>

                    <tbody>
                    <tr>
                        {{ include('ano_lectivo/_delete_form.html.twig') }}
                        <a href="{{ path('ano_lectivo_index') }}" class="btn btn-sm btn-primary mr-2 ml-2"><i class="fas fa-list"></i>Voltar a Lista</a>
                        <a href="{{ path('ano_lectivo_edit', {'id': ano_lectivo.id}) }}" class="btn btn-sm btn-secondary"><i class="fas fa-edit"></i>Editar</a>

                    </tr>
                    <tr>
                        <th>Nome Ano Lectivo</th>
                        <td>{{ ano_lectivo.nomeAnoLectivo }}</td>
                    </tr>
                    <tr>
                        <th>Data Início</th>
                        <td>{{ ano_lectivo.dataInicio ? ano_lectivo.dataInicio|date('Y-m-d') : '' }}</td>
                    </tr>

                    </tbody>
                </table>

            </div>
        </div>
    </div>

</div>



{% endblock %}


