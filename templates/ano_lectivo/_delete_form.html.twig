<form method="post" action="{{ path('ano_lectivo_delete', {'id': ano_lectivo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ano_lectivo.id) }}">
    <button class="btn btn-sm btn-danger"><i class="fa-trash-trash"></i> Delete</button>
</form>
