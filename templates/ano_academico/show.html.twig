{% extends 'base.html.twig' %}

{% block title %}Mostrar Ano Academico{% endblock %}

{% block body %}
<div class="col-sm-8">
    <div class="card">
        <div class="card-header">

            <h4> Ano Acadêmico {{ ano_academico.nomeAnoAcademico}}</h4>
            <div class="card-body">
                <table class="table table-sm">
                    <tbody>

                    <tbody>
                    <tr>
                        {{ include('ano_academico/_delete_form.html.twig') }}
                        <a href="{{ path('ano_academico_index') }}" class="btn btn-sm btn-primary mr-2 ml-2"><i class="fas fa-list"></i>Voltar a Lista</a>
                        <a href="{{ path('ano_academico_edit', {'id': ano_academico.id}) }}" class="btn btn-sm btn-secondary"><i class="fas fa-edit"></i>Editar</a>
                    </tr>

                    <tr>
                        <th>Nome Ano Acadêmico</th>
                        <td>{{ ano_academico.nomeAnoAcademico}}</td>
                    </tr>


                    </tbody>
                </table>

            </div>
        </div>
    </div>

</div>



{% endblock %}

